[package]
name = "uesave_wasm"
version = "0.1.0"
edition = "2021"
license = "MIT-0"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# ✅ No features here; just pin the crate version you want
uesave = "0.6.2"

